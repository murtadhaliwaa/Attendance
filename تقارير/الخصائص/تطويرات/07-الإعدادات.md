# 07 — الإعدادات

## الوصف
إدارة الشفتات والأقسام ومستخدمي النظام.

## المسار
`/dashboard/settings` — يتطلب `settings:read`

## المكوّنات

| الملف | الدور |
|-------|-------|
| `shifts-settings.tsx` | CRUD شفتات |
| `departments-settings.tsx` | CRUD أقسام |
| `settings/page.tsx` | SSR |

## API

| المسار | الصلاحية |
|--------|----------|
| `/api/schedules` | read/write |
| `/api/schedules/[id]` | write |
| `/api/departments` | read/write |
| `/api/departments/[id]` | write |

## الشفتات الافتراضية

`lib/shift-defaults.ts` — 3 شفتات  
`lib/shifts.ts` → `ensureDefaultShifts()` — يُستدعى عند الحاجة

## ملاحظة مهمة

جدول `Department` **منفصل** عن حقل `Employee.department` — انظر مخالفات الممارسات.

## كتابة الإعدادات

تتطلب `settings:write` — **MANAGER فقط**
