# خطة التحسين المقترحة

## المرحلة 1 — استقرار (1–2 أسابيع)

- [ ] تحديث `download-face-models.js` ليشمل TinyFaceDetector + landmark68Tiny
- [ ] إضافة `verifyFaceMatchStrict()` على الخادم يستخدم `selectBestFaceMatch`
- [ ] حذف `auth-cookies.ts` و `xlsx` غير المستخدم
- [ ] إنشاء `lib/session.ts` لتوحيد تحقق المصادقة
- [ ] تحديث README.md للمشروع

## المرحلة 2 — بنية (2–4 أسابيع)

- [ ] Prisma migrations بدل `db push` فقط
- [ ] توحيد الأقسام: `Employee.departmentId` → FK
- [ ] إكمال `/api/alerts` أو إزالة الـ stub
- [ ] تجميع `lib/` في مجلدات فرعية (face/ · employees/ · reports/)

## المرحلة 3 — أمان وتوسع (شهر+)

- [ ] Rate limiting موزع (Upstash)
- [ ] مراجعة أمان مفتاح الكشك
- [ ] اختبارات تكامل للوجه والصلاحيات
- [ ] تقييم بديل face-api.js

## عند كل ميزة جديدة

1. اتبع `الهيكلية-الصحيحة/دليل-إضافة-ميزة-جديدة.md`
2. لا تُضف ديناً — أو سجّله هنا فوراً
3. حدّث `ارتباطات-الملفات.md`
