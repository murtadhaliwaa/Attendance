# الديون التقنية

| # | المشكلة | الخطورة | الملفات | الإصلاح المقترح |
|---|---------|---------|---------|-----------------|
| 1 | تحقق خادم للوجه أضعف من العميل (لا gap) | ~~متوسطة~~ **مُصلَح** | verifyAttendanceFaceMatch | — |
| 2 | مفتاح الكشك مرئي في العميل | ~~متوسطة~~ **مُصلَح** | kiosk-session · middleware · kiosk-client | جلسة HttpOnly + BFF |
| 3 | Rate limit ذاكرة محلية | ~~منخفضة~~ **مُحسَّن** | rate-limit.ts | Upstash عند التهيئة، وإلا ذاكرة محلية |
| 4 | لا migrations في Git | ~~متوسطة~~ **مُضاف** | prisma/migrations/ | `migrate deploy` على الإنتاج |
| 5 | تعارض React 19 من المجلد الأب | منخفضة (مُصلَح) | next.config.mjs | webpack modules priority |
| 6 | `/api/alerts` stub 501 | ~~منخفضة~~ **مُصلَح** | alerts/route.ts | GET + PATCH |
| 7 | ~~face-api.js قديم~~ | ~~متوسطة~~ **مُصلَح** | human-engine.ts | Human 1024-d — حُذف face-api/MediaPipe |
| 8 | README افتراضي Next.js | ~~منخفضة~~ **مُصلَح** | README.md | توثيق المشروع |
| 9 | بصمات seed وهمية | منخفضة | seed.ts · face-descriptor-utils | لا seed faces في إنتاج |
| 10 | `public/models/human/` | عالية للتشغيل | public/models/human | `npm run models:download` قبل النشر/التابلت |
| 11 | Department مزدوج | ~~منخفضة~~ **مُحسَّن** | schema · department-resolve | `departmentId` + backfill |
| 12 | auth-cookies.ts ميت | ~~منخفضة~~ **مُصلَح** | — | حُذف |
| 13 | kiosk-scanner.tsx ضخم | ~~متوسطة~~ **مُصلَح** | hooks · kiosk-result-panel | تقسيم إلى hooks ومكوّنات |
| 14 | PWA SW فقط إنتاج | معلوماتية | pwa-register.tsx | — |
| 15 | بيانات دخول تجريبية في dev | معلوماتية | login/page.tsx | — |

## أولويات الإصلاح

### عاجل (قبل ميزات كبيرة)
1. ~~إكمال سكربت تحميل النماذج~~
2. ~~توحيد تحقق الوجه على الخادم~~
3. تطبيق migration `department_id` على قاعدة الإنتاج ثم `npm run db:backfill-departments`

### متوسط
4. ~~Prisma migrations في Git~~
5. ~~حذف كود ميت~~
6. ~~README المشروع~~
7. تهيئة Upstash في Vercel للإنتاج (اختياري)

### لاحق
8. ~~استبدال face-api.js~~ — **تم (Human 1024-d + نماذج محلية)**
9. تقييد IP إضافي لمسارات الكشك إن لزم
